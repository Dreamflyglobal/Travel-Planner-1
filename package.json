{
  "name": "travel-app",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @workspace/travel-booking run build && pnpm --filter @workspace/api-server run build",
    "start": "NODE_ENV=production node artifacts/api-server/dist/index.mjs",
    "dev": "node server.js",
    "db:push": "pnpm --filter @workspace/db run push",
    "db:generate": "pnpm --filter @workspace/db run generate",
    "db:migrate": "pnpm --filter @workspace/db run migrate",
    "db:seed": "pnpm --filter @workspace/db run seed",
    "typecheck": "pnpm --filter @workspace/api-server run typecheck && pnpm --filter @workspace/travel-booking run typecheck"
  }
}
